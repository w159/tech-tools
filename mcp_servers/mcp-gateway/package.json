{
  "name": "mcp-gateway",
  "version": "0.1.0",
  "description": "Henssler Financial remote MCP gateway - Entra-ID resource server that aggregates vendor MCP stdio backends over Streamable HTTP",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run --dir src"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.3",
    "jose": "^6.2.2"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
