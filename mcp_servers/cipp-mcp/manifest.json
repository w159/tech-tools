{
  "manifest_version": "0.3",
  "name": "cipp-mcp",
  "version": "0.2.3",
  "display_name": "CIPP MCP Server",
  "description": "Manage M365 tenants, users, security policies, and standards via CIPP (CyberDrain Improved Partner Portal) — for MSP AI agents.",
  "long_description": "Use this server when you need to automate Microsoft 365 management tasks across multiple MSP client tenants through CIPP. Covers user lifecycle (create, edit, reset passwords, MFA, licenses), group management, mailbox and shared-mailbox operations, conditional access policies, Defender and Entra security standards, compliance baselines, and CIPP alert/audit workflows. Supports both static Bearer token and OAuth client-credentials authentication. Call tools to query or act on any tenant accessible from your CIPP deployment.",
  "author": {
    "name": "Aaron Sachs",
    "url": "https://github.com/asachs01"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/w159/tech-tools"
  },
  "homepage": "https://github.com/w159/tech-tools/tree/main/mcp_servers/cipp-mcp",
  "license": "Apache-2.0",
  "keywords": ["cipp", "m365", "microsoft365", "msp", "multi-tenant", "azure"],
  "server": {
    "type": "node",
    "entry_point": "dist/entry.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/dist/entry.js"],
      "env": {
        "CIPP_BASE_URL": "${user_config.cipp_base_url}",
        "CIPP_API_KEY": "${user_config.cipp_api_key}",
        "CIPP_TENANT_ID": "${user_config.cipp_tenant_id}",
        "CIPP_CLIENT_ID": "${user_config.cipp_client_id}",
        "CIPP_CLIENT_SECRET": "${user_config.cipp_client_secret}",
        "MCP_TRANSPORT": "stdio"
      }
    }
  },
  "tools_generated": true,
  "user_config": {
    "cipp_base_url": {
      "type": "string",
      "title": "CIPP Base URL",
      "description": "The base URL of your self-hosted CIPP deployment (e.g. https://cipp.yourdomain.com). Required — CIPP is self-hosted and has no vendor default URL.",
      "required": true
    },
    "cipp_api_key": {
      "type": "string",
      "title": "CIPP API Key (static Bearer token — legacy)",
      "description": "Only required if using a static Bearer token. Leave blank to use OAuth client-credentials via the fields below.",
      "required": false,
      "sensitive": true
    },
    "cipp_tenant_id": {
      "type": "string",
      "title": "Entra Tenant ID",
      "description": "Entra tenant (directory) ID that owns the CIPP API-client app registration. Required for OAuth client-credentials auth.",
      "required": false
    },
    "cipp_client_id": {
      "type": "string",
      "title": "CIPP API Client ID",
      "description": "OAuth client ID from CIPP → Settings → Integrations → CIPP-API.",
      "required": false
    },
    "cipp_client_secret": {
      "type": "string",
      "title": "CIPP API Client Secret",
      "description": "OAuth client secret paired with the Client ID.",
      "required": false,
      "sensitive": true
    }
  },
  "compatibility": {
    "platforms": ["darwin", "win32", "linux"],
    "runtimes": {
      "node": ">=18.0.0"
    },
    "claude_desktop": ">=0.10.0"
  }
}
