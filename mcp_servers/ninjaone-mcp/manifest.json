{
  "manifest_version": "0.3",
  "name": "ninjaone-mcp",
  "display_name": "NinjaOne MCP Server",
  "version": "1.8.0",
  "description": "Query and manage NinjaOne RMM data \u2014 devices, organizations, tickets, alerts, and software \u2014 for MSP AI agents.",
  "long_description": "Use this server when you need to automate or query NinjaOne RMM. List and filter devices by organization, class, or online status; get device details and installed software; reboot devices; manage Windows services; list organizations and their device counts; search and manage helpdesk tickets; and acknowledge or resolve alerts. Uses OAuth2 client_credentials. Call ninjaone_navigate first to load the tools for a specific domain.",
  "author": {
    "name": "w159",
    "url": "https://github.com/w159"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/w159/tech-tools"
  },
  "homepage": "https://github.com/w159/tech-tools/tree/main/mcp_servers/ninjaone-mcp",
  "license": "Apache-2.0",
  "keywords": [
    "ninjaone",
    "rmm",
    "msp",
    "devices",
    "it-management"
  ],
  "server": {
    "type": "node",
    "entry_point": "dist/mcp_servers/ninjaone-mcp/src/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/dist/mcp_servers/ninjaone-mcp/src/index.js"
      ],
      "env": {
        "NINJAONE_CLIENT_ID": "${user_config.ninjaone_client_id}",
        "NINJAONE_CLIENT_SECRET": "${user_config.ninjaone_client_secret}",
        "NINJAONE_REGION": "${user_config.ninjaone_region}",
        "NINJAONE_AUTH_MODE": "${user_config.ninjaone_auth_mode}",
        "NINJAONE_BASE_URL": "${user_config.ninjaone_base_url}",
        "MCP_TRANSPORT": "stdio"
      }
    }
  },
  "tools_generated": true,
  "user_config": {
    "ninjaone_client_id": {
      "type": "string",
      "title": "NinjaOne Client ID",
      "description": "Your NinjaOne OAuth client ID. Required for both auth modes.",
      "sensitive": true,
      "required": true
    },
    "ninjaone_client_secret": {
      "type": "string",
      "title": "NinjaOne Client Secret",
      "description": "Required when auth mode is client_credentials. Ignored in user mode (browser sign-in). Leave blank if using user mode.",
      "sensitive": true,
      "required": false
    },
    "ninjaone_region": {
      "type": "string",
      "title": "NinjaOne Region",
      "description": "Region code that auto-derives the base URL. Default us \u2192 https://app.ninjarmm.com. Valid values: us, eu (https://eu.ninjarmm.com), ca, oc, us2, fed. Change only if your NinjaOne instance is on a non-US shard.",
      "sensitive": false,
      "required": false,
      "default": "us"
    },
    "ninjaone_auth_mode": {
      "type": "string",
      "title": "Auth mode",
      "description": "client_credentials (default): paste a Client ID + Secret from a NinjaOne OAuth app and the server mints tokens automatically. user: skip the secret; instead run the `ninjaone_sign_in` tool in chat to open NinjaOne in your browser, sign in once, and the refresh token is stored to ~/.atlas/ninjaone-tokens.json. User mode requires your OAuth app to list http://127.0.0.1:53682/oauth/callback as an allowed redirect URI.",
      "required": false,
      "default": "client_credentials"
    },
    "ninjaone_base_url": {
      "type": "string",
      "title": "NinjaOne Base URL",
      "description": "Optional. Leave blank to use the vendor default (https://app.ninjarmm.com). Only set for non-US regions or staging shards (e.g. https://eu.ninjarmm.com). The ninjaone_region field already auto-derives the correct regional URL \u2014 only set this to override it explicitly.",
      "sensitive": false,
      "required": false,
      "default": ""
    }
  },
  "compatibility": {
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=18.0.0"
    },
    "claude_desktop": ">=0.10.0"
  }
}
