# Findings index

Durable, dated learning ledger. Each resolved issue, fix, or decision
gets one `<YYYY-MM-DD>-<slug>.md` file in this folder; this index links
them so an agent can check for prior art before starting non-trivial
work, without re-reading every finding file.

## How to use

- Before non-trivial work, scan this index for entries touching the same
  area before starting; the fix may already exist.
- After a verified fix, add a row here in the same change that adds the
  dated finding file.

## Index

| Date | Slug | Area | Summary |
|---|---|---|---|
| 2026-07-17 | atlas-canonical-structure-scaffolding | atlas-setup, docs-ssot, docs-curator, docs-auditor, session_boot, atlas-gitignore | `atlas-setup` only scaffolded a partial `docs/`+`.atlas/` tree while the docs-ssot refs, docs-curator, and session_boot advisory already assumed the full 25-path canonical structure existed, so older-scaffolded repos silently drifted from what the rest of the fleet expected. Fixed by making the canonical structure one definition, mirrored byte-identical across both docs-ssot references, scaffolded/repaired idempotently by `scaffold_docs.py`, and enforced consistently by docs-curator (owner), docs-auditor (read-only checker), session_boot (advisory), and atlas-gitignore (zero-trust seed + validator). See `.atlas/findings/2026-07-17-atlas-canonical-structure-scaffolding.md`. |
| 2026-07-17 | mcp-shared-build-break-resolved | mcp_servers/_shared, blumira-mcp, threatlocker-mcp, vanta-mcp | `mcp_servers/_shared/` was deleted in `56d1a9f`, breaking the build for three servers with no local fallback (`@shared/*` imports unresolved, e.g. `mcp_servers/threatlocker-mcp/src/domains/_helpers.ts:15,21,26`). Fixed by commit `adace06`, which restored the 9-file top-level directory. Note: `auvik-mcp`, `connectwise-manage-mcp`, and `cipp-mcp` still carry separate private per-server copies - consolidation remains open (see `docs/ROADMAP.md` DRY-divergence item). See `.atlas/findings/2026-07-17-mcp-shared-build-break-resolved.md`. |
| 2026-07-21 | remove-m365-vendor-assessment | plugins/atlas/skills | Deleted the unused `atlas-m365` and `atlas-vendor-assessment` skills (no callers; `atlas-m365` overlapped armada's own M365 coverage). Atlas plugin skill count: 22 -> 20. See `.atlas/findings/2026-07-21-remove-m365-vendor-assessment.md`. |
| 2026-07-22 | kimi-marketplace-manifest-fix | kimi-plugin, marketplace, plugins/atlas, plugins/armada, plugins/programmer | Kimi marketplace installation was broken: armada and programmer plugins missing `.kimi-plugin/plugin.json` manifests; root `kimi.plugin.json`, `.kimi-plugin/marketplace.json`, and `marketplace.json` used local paths instead of GitHub URLs. Added missing manifests and updated all three files with GitHub source URLs. All 3 plugins (atlas, armada, programmer) now installable via Kimi marketplace. See `.atlas/findings/2026-07-22-kimi-marketplace-manifest-fix.md`. |
| 2026-07-21 | programmer-plugin-move | plugins/programmer, .claude-plugin/marketplace.json | Moved the standalone `pragmatic-programmer` plugin into the atlas marketplace as `programmer` (skills renamed `tpp-*`). Marketplace catalog: `3.0.0` -> `3.1.0`, now lists `atlas`, `armada`, `programmer`. Verified CONFIRMED by two independent verifier passes after fixing one stale `LICENSE:3` reference. See `.atlas/findings/2026-07-21-programmer-plugin-move.md` and `.atlas/evidence/2026-07-21-programmer-plugin-move.md`. |
| 2026-07-28 | stop-hook-memory-capture-loop | plugins/atlas/hooks/memory_capture.py, ingest_session.py, auto_skill.py, nudge.py, completion_gate.py, plugins/atlas/scripts/session_ingest.py, atlas_hook_guard.py | A Stop-hook self-improvement loop burned a usage limit. Stage one (`ab67df4`): per-hook `stop_hook_active` guard, content-hash dedupe + 900s throttle in `memory_capture.py`, a machine-authored-text filter in `session_ingest.py` with a line-start anchor and quote-prefix strip so markdown-blockquoted hook output is also caught. Judged insufficient because the invariant was hand-copied per hook and any new hook would inherit none of it. Stage two, atlas 5.2.0: centralized into shared module `atlas_hook_guard.py` (should_run/emit) plus a session-wide circuit breaker (5 Stop events in 120s trips it for the rest of the session); `completion_gate.py` deliberately uses should_run() only, not emit(), so its repeat-until-fixed block message is not deduped away. 84 passed across the four stage-one hook test files; 101 passed in `test_session_ingest.py`; 23 passed in `test_atlas_hook_guard.py`; 427 passed in `plugins/atlas/hooks`; 562 passed in `plugins/atlas/scripts`. No open gap remains from this work; wholesale suppression in `_is_machine_authored()` is a documented, deliberate, pre-existing trade-off. See `.atlas/findings/2026-07-28-stop-hook-memory-capture-loop.md`. |

| 2026-08-05 | stop-hook-active-silenced-capture-hooks | atlas_hook_guard.py, ingest_session.py, memory_capture.py, chronicle_facet.py | `should_run()`'s unconditional `stop_hook_active` short-circuit silenced capture hooks (ingest_session, memory_capture, chronicle_facet) on every completion_gate block, not just message-repeating hooks -- the direct cause of `improvements`/`asset_verdicts` sitting weeks stale. Fixed with a `kind="capture"|"emit"` parameter; only `"emit"` hooks are gated. See `.atlas/findings/2026-08-05-stop-hook-active-silenced-capture-hooks.md`. |
| 2026-08-05 | memory-cap-silently-dropped-lessons | atlas_memory.py, memory_capture.py | `MEMORY.md`'s 4000-byte `WORKING_CAP_CHARS` silently dropped every lesson since 2026-07-16 once the file exceeded it (`atlas_memory.add()` returned `success=False` with no caller-side handling). Fixed: cap raised to 20,000 with rotation-to-archive on overflow, plus `_record_drop()` so an unstorable lesson now lands in `friction_events` instead of vanishing. See `.atlas/findings/2026-08-05-memory-cap-silently-dropped-lessons.md`. |
| 2026-08-05 | gitignore-secret-patterns-above-allowlist | .gitignore | SECURITY, pre-existing: secret patterns (`*.key`, `*.pem`, `id_rsa`, `credentials.json`, etc.) sat above the `!docs/**`/`!.atlas/**` allowlist, so every later allowlist rule re-admitted them (last-rule-wins). Verified trackable before fix: `docs/decisions/{secret.key,foo.pem,id_rsa,credentials.json}`, `docs/audits/secret.key`, `docs/specs/id_rsa`; only `.env` was safe. Affected `docs/audits/` and `docs/specs/` identically, not just the folder most recently edited. Fixed with a post-allowlist "Global secret re-exclusion" block at `.gitignore:341-371`. Pattern-coverage completeness (beyond ordering) is a separate, ongoing verifier audit. See `.atlas/findings/2026-08-05-gitignore-secret-patterns-above-allowlist.md`. |
| 2026-08-06 | carried-gap-closure-5.6.0 | completion_gate.py, chronicle_facet.py, test_connectors_wiring.py, atlas_doctor.py, skill_factory.py (deleted), .gitignore | Six gaps that had been recorded and left open, closed together. `facets.gate_block_count` was NULL because nothing wrote a row; wiring it exposed `_sync_friction_events` deleting friction rows other hooks own (`gate_block`, `memory_drop`). The gate could not distinguish "this run wrote nothing" from "nothing was recorded", so a telemetry-less session got a gate enforcing only "the docs files exist". `test_connectors_wiring.py` still globbed `*.mcpb` after the 2026-07-31 ESM move, making three tests vacuous and one the repo's standing failure. Ten dump/keystore shapes (`*.pgdump`, `*.dmp`, `*.rdb`, `*.bacpac`, `*.sqlite*`, `*.db`, `*.jceks`, `*.keytab`, `*.p7b`) were trackable inside allowlisted folders. `skill_factory.py` outlived the hook 5.5.0 unwired. One ROADMAP gap was already fixed and never struck. Reusable rule: a deferred gap needs a test that asserts it, not a paragraph. See `.atlas/findings/2026-08-06-carried-gap-closure-5.6.0.md` and `.atlas/evidence/2026-08-06-atlas-5.6.0-gap-closure.md`. |

| 2026-08-06 | subagent-mcp-routing-and-tiers | plugins/atlas/agents/*.md (all 12), skills/atlas-orchestrate/SKILL.md, references/{subagent-kit,capability-routing,prompt-optimization}.md, hooks/test_atlas_contract.py | Atlas subagents never called serena, lean-ctx, context-mode, or claude-mem. Agent bodies named them in prose, but they are deferred MCP tools whose schemas are absent from a subagent's tool list until it calls `ToolSearch` -- so the agent found nothing and silently fell back to `Grep`+`Read`. Three agents also carried a `tools:` frontmatter allowlist, which revokes every `mcp__*` tool by construction. `capability-routing.md`'s routing table had existed the whole time and changed nothing, because nothing on the dispatch path read it. Fixed with a concrete per-role tool table (exact names + ToolSearch-by-keyword) in all 12 agents, the three allowlists removed, and the rule moved into the prompt feed that actually runs per dispatch. Tiers tuned in the same pass: `effort` declared on every agent (low for the nine spec-executing roles, medium for the three that render an independent verdict), sonnet as the ceiling, opus reserved for the orchestrator. Reusable rule: a tool named in prose is not a tool the agent can call, and a reference that does not sit on the execution path is not enforcement. See `.atlas/findings/2026-08-06-subagent-mcp-routing-and-tiers.md`. |
| 2026-08-11 | serena-never-activated | ~/.mcp.json, .serena/project.yml, plugins/atlas/agents/*.md (all 12), references/{capability-routing,lsp-and-symbols,subagent-kit}.md, hooks/test_atlas_contract.py | 5.7.0 made the agents name serena correctly and serena still did nothing, because two config defects sat in series underneath. serena 1.6 made `languages:` a `ProjectConfig` field without a default; every `.serena/project.yml` on the machine predates it and carries only `language_servers:`, so `_from_dict():569` raises `KeyError: 'languages'` and the project is skipped at load. Separately `~/.mcp.json:57` launched the server with `--context claude-code` and no `--project`, so nothing activated even where the yml was valid. Together: an always-empty dashboard and a 29% tool error rate (vs 4.6% context-mode, 6.8% lean-ctx) that read as a bad tool. The first determination -- "native `LSP` subsumes serena, remove it" -- was wrong: `LSP` takes a position and returns locations, `find_symbol` takes a name and returns a body, and serena's symbol-edit tools (`replace_symbol_body`, `rename_symbol`, `replace_in_files`) have no native equivalent; the `claude-code` context excludes `read_file`/`search_for_pattern`/etc. so serena never overlapped lean-ctx at all. Fixed: `languages:` key, `--project-from-cwd`, a `NON-INTERACTIVE` dispatch clause overriding serena's `interactive` default mode, and a single up-front batched `ToolSearch` in all 12 agents. Reusable rule: a tool that is wired, named, and still unused is a configuration failure until proven a capability failure -- and judge a server by its manual and context definition, not its tool list. See `.atlas/findings/2026-08-11-serena-never-activated.md`. |

atlas:docs-curator maintains this file. atlas-setup only creates it.
